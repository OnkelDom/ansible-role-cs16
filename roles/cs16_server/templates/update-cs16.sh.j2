#!/bin/sh
set -eu
systemctl stop cs16.service
sudo -u {{ cs16_user }} {{ cs16_steamcmd_dir }}/steamcmd.sh \
  +force_install_dir {{ cs16_server_dir }} \
  +login anonymous \
  +app_set_config 90 mod cstrike \
  +app_update 90 validate \
  +quit
systemctl start cs16.service

